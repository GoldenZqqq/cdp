# Spec and Documentation Refresh Implementation

1. Rewrite all backend/frontend placeholder specs from current source/test
   evidence and update both indexes.
2. Add the dependency-free documentation validator and negative Node fixtures;
   wire them into `tests/web` asset-quality execution.
3. Add mirrored state-file and development/CI sections to both READMEs, then
   refresh CONTRIBUTING and AGENTS architecture/commit guidance.
4. Replace duplicated CLAUDE instructions with a concise canonical AGENTS link.
5. Update changelog, progress, release notes, task records, and deterministic
   Scoop package hash.
6. Run documentation fixtures/current gate, full Web suite, PowerShell quality,
   package/generated shell checks, YAML/JSON, placeholder search, Trellis, and
   whitespace validation.

## Risk and Rollback Points

- Run the documentation gate against the current READMEs before wiring CI; tune
  structural assertions only when they reject a legitimate translation shape.
- Parse manifest export arrays rather than maintaining a copied command list.
- Keep README changes paired and review their heading/code-block sequences after
  every edit.
- Preserve AGENTS release rules while replacing only stale architecture and
  commit-format guidance.

## Completion Record

- [x] Rewrote all backend/frontend template specs from current source and tests;
  updated both indexes with pre-development and quality boundaries.
- [x] Added the dependency-free documentation validator and five focused
  positive/negative Node fixtures, wired into Web CI execution.
- [x] Synchronized state/persistence and current quality commands in both
  READMEs while preserving heading and code-block structure.
- [x] Refreshed CONTRIBUTING and AGENTS; reduced CLAUDE.md to the canonical
  AGENTS entry point.
- [x] Updated changelog, progress, ReleaseNotes, Scoop hash, and task records.
- [x] Passed documentation, Web, PowerShell, package, generated shell,
  YAML/JSON, placeholder, Trellis, and whitespace validation.
