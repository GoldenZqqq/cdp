---
format: 1920x1080
message: "cdp — your project workbench in the terminal. One command to see all repo statuses, switch projects, and launch AI CLIs."
arc: Hook → Pain → Solution → Feature-Benefit Cascade → CTA
audience: terminal developers managing multiple repositories
mode: autonomous
music: focused electronic, dev-tool energy
---

## Frame 1 — Pain

- scene: Terminal chaos — endless cd commands scrolling
- duration: 5s
- transition_in: cut
- status: outline
- type: hook

Dark terminal. Lines of `cd ../project1 && git status && cd ../project2 && git status` scroll rapidly. Text appears: "50 repos. Which ones have uncommitted code?" The chaos freezes.

## Frame 2 — Solution: cdp status

- scene: cdp status dashboard appears with color-coded table
- duration: 6s
- transition_in: crossfade
- status: outline
- type: feature_showcase

Terminal clears. Types `cdp status`. A clean table reveals row by row: project names, branches, dirty/clean status (green +, red x), ahead/behind sync, last commit times. Summary line: "3 repos need attention". The killer feature moment.

## Frame 3 — Quick Switch

- scene: cdp api instant switch with fzf preview
- duration: 5s
- transition_in: cut
- status: outline
- type: feature_showcase

Types `cdp api`. Terminal instantly switches to my-api project root. Tab title updates. Then types `cdp api -Open codex` — switches AND launches Codex in one move.

## Frame 4 — Workspace

- scene: cdp workspace fullstack opens multiple tabs
- duration: 4s
- transition_in: cut
- status: outline
- type: feature_showcase

Types `cdp workspace fullstack`. Three terminal tabs spring open simultaneously: api, frontend, docs. Each tab titled with the project name. One command, full stack ready.

## Frame 5 — Smart Features

- scene: onEnter + Tab completion montage
- duration: 4s
- transition_in: cut
- status: outline
- type: benefit_highlight

Split view: Left shows `cdp ml-project` → auto-activates virtualenv. Right shows `cdp s<TAB>` → completion dropdown appears with status, scan. Smart terminal that knows your projects.

## Frame 6 — Cross Platform

- scene: Platform icons with same cdp command
- duration: 3s
- transition_in: crossfade
- status: outline
- type: branding

Three terminal windows side by side: PowerShell (Windows), zsh (macOS), bash (Linux). Each shows `cdp status` with identical output. Same tool, everywhere.

## Frame 7 — Install + CTA

- scene: Install one-liner with GitHub stars call
- duration: 5s
- transition_in: crossfade
- status: outline
- type: cta

Two install commands appear with typing animation:
`Install-Module -Name cdp` (PowerShell)
`brew install fzf jq && curl ... | bash` (macOS/Linux)
Then the cdp logo + tagline: "Your project workbench in the terminal"
GitHub URL: github.com/GoldenZqqq/cdp
